@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model License_Key_Shop_Web.Models.Category;
@{
    ViewData["Title"] = "Create";
    Layout = "~/Pages/Shared/_LayoutCustomer.cshtml";
}

<!-- Add Category Start -->
<div class="container-fluid pt-5" style="margin-top: 100px; margin-bottom: 50px">
    <div class="container">
        <div class="row g-5">
            @{
                if (ViewBag.cateList != null)
                {
                    <div class="col-lg-5">
                        <div class="border-start border-5 border-success ps-5 mb-5" style="max-width: 600px;">
                            <h1 class="display-5 text-uppercase mb-0">Current</h1>
                        </div>
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th scope="col">ID</th>
                                    <th scope="col">Category Name</th>
                                    <th scope="col" class="text-center">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    foreach (var item in ViewBag.cateList)
                                    {
                                        <tr>
                                            <form asp-action="Update" asp-controller="Category" method="post">
                                            <td scope="row">@(item.CategoryId)</td>
                                            <td scope="row">
                                                <input type="text" class="form-control" name="categoryName" value="@(item.CategoryName)" />
                                                <input type="hidden" name="categoryID" value="@(item.CategoryId)" />
                                            </td>
                                            <td scope="row" class="text-center">
                                                <button class="btn btn-primary" type="submit">Update</button>
                                                <a asp-action="Delete" asp-controller="Category" asp-route-id="@item?.CategoryId" class="btn btn-danger">Delete</a>
                                            </td>
                                            </form>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                }
            }
            <div class="col-lg-2"></div>
            <div class="col-lg-5">
                <div class="border-start border-5 border-success ps-5 mb-5" style="max-width: 600px;">
                    <h1 class="display-5 text-uppercase mb-0">Add category</h1>
                </div>
                <form asp-action="Create" asp-controller="Category" method="post">
                    <div class="row g-3">
                        <div class="col-12">
                            <div class="row">
                                <div class="col-8">
                                    <input name="categoryName" placeholder="Category name" type="text" class="form-control bg-light border-2 px-4" style="height: 55px;" pattern="^(?=.*[a-z])(?=.*[A-Z]).{3,25}$" title="Category name can only contain 3 to 25 lowercase letters and numbers." required>
                                </div>
                                <div class="col-4">
                                    <button class="btn btn-success w-50 py-3" type="submit">Add</button>
                                </div>
                            </div>
                            @{
                                if (ViewBag.AddCategoryErr != null)
                                {
                                    <span style="color: red; font-style: italic">*@(ViewBag.AddCategoryErr)</span>
                                }
                            }
                            @{
                                if (ViewBag.AddCategorySuccess != null)
                                {
                                    <span style="color: green; font-style: italic">*@(ViewBag.AddCategorySuccess)</span>
                                }
                            }
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<!-- Add Category End -->